apply plugin: "war"

bootWar {
    mainClassName = "br.gov.serpro.pgt.backend.PgtBackendApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
